// Re-export everything from the new modular structure for backwards compatibility
// This file maintains the same import path for all consumers

export { useNeedManager } from './need-manager/index';

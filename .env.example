# Gemini API Key (Server-side only - NEVER commit the actual .env file)
GEMINI_API_KEY=your_gemini_api_key_here

# App Password (for production authentication)
APP_PASSWORD=your_secure_password_here

# Optional: API URL for development
# Leave empty for production (defaults to same domain)
# VITE_API_URL=http://localhost:5173
